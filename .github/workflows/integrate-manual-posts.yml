name: Integrate manual posts into CardRadar

on:
  push:
    branches: [main]
    paths:
      - manual_posts.json
      - assets/manual-posts-integration.js
      - assets/manual-posts-fallback-fix.css
      - .github/workflows/integrate-manual-posts.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Ensure integration assets are loaded
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path('osaka-nihonbashi.html')
          html = path.read_text(encoding='utf-8')
          style_marker = '<link rel="stylesheet" href="assets/manual-posts-fallback-fix.css?v=20260722-2">'
          script_marker = '<script src="assets/manual-posts-integration.js?v=20260722"></script>'

          if style_marker not in html:
              if '</head>' not in html:
                  raise SystemExit('osaka-nihonbashi.html has no closing head tag')
              html = html.replace('</head>', f'{style_marker}\n</head>', 1)

          if script_marker not in html:
              if '</body>' not in html:
                  raise SystemExit('osaka-nihonbashi.html has no closing body tag')
              html = html.replace('</body>', f'{script_marker}\n</body>', 1)

          path.write_text(html, encoding='utf-8')
          PY

      - name: Commit generated integration
        run: |
          if git diff --quiet -- osaka-nihonbashi.html; then
            echo "No integration change needed"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add osaka-nihonbashi.html
          git commit -m "Integrate manual posts into CardRadar"
          git push
